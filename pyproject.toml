[project]
name = "arch-mcp"
version = "0.2.0"
description = "MCP server for Python API architecture controls"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastmcp>=3.0.0b1",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "ruff>=0.8.0",
]

[project.scripts]
arch-mcp = "arch_mcp.server:main"
arch-mcp-scaffold = "arch_mcp.scaffold:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/arch_mcp"]

[tool.hatch.build.targets.wheel.force-include]
"src/arch_mcp/default_rules.yaml" = "arch_mcp/default_rules.yaml"

[tool.hatch.build.targets.sdist]
include = ["src/", "rules/"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
